import{_ as r,r as o,o as d,c as l,a as s,b as e,d as n,w as c,e as a}from"./app-nZBleB1D.js";const p={},u=a(`<h1 id="sas-cli-reference" tabindex="-1"><a class="header-anchor" href="#sas-cli-reference" aria-hidden="true">#</a> <code>sas</code> CLI reference</h1><p>The <code>sas</code> command is a thin wrapper around the local S&amp;S HTTP API. It auto-discovers tools from <code>/api/v1/actions</code>, so every tool registered in the app is available as a CLI subcommand <em>with zero CLI rebuild</em>.</p><h2 id="install" tabindex="-1"><a class="header-anchor" href="#install" aria-hidden="true">#</a> Install</h2><p><strong>macOS</strong> — the installer runs automatically on first launch:</p><ol><li>Launch Signals &amp; Sorcery.</li><li>On the final wizard screen (&quot;You&#39;re all set!&quot;) leave <strong>&quot;Add <code>sas</code> command to your terminal&quot;</strong> checked.</li><li>Approve the admin prompt. The app writes a small wrapper to <code>/usr/local/bin/sas</code> that runs the CLI through the app&#39;s own bundled runtime — you don&#39;t need Node.js installed.</li><li>Open a <strong>new</strong> terminal window (your existing shells don&#39;t inherit PATH changes).</li></ol><p>Want to install later, reinstall after moving the app, or remove it? <strong>Settings → Developer Tools → sas CLI</strong> has Install / Reinstall / Uninstall buttons that read the current status and kick off the same one-prompt flow.</p><h3 id="if-you-decline-the-admin-prompt" tabindex="-1"><a class="header-anchor" href="#if-you-decline-the-admin-prompt" aria-hidden="true">#</a> If you decline the admin prompt</h3><p>We don&#39;t leave you empty-handed. If you click <strong>Cancel</strong> on the admin dialog, the app offers to install for just your account — no admin required. The wrapper lands at <code>~/.local/bin/sas</code> instead of <code>/usr/local/bin/sas</code>. Two follow-ups you&#39;ll need to do yourself:</p><ol><li><p><strong>Add <code>~/.local/bin</code> to your PATH.</strong> It&#39;s not on PATH by default on macOS. Add this to your shell profile (<code>~/.zshrc</code>, <code>~/.bashrc</code>, or equivalent):</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token string">&quot;<span class="token environment constant">$HOME</span>/.local/bin:<span class="token environment constant">$PATH</span>&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>…then open a new terminal and <code>sas --version</code> should work.</p></li><li><p><strong>Or invoke by full path:</strong> <code>~/.local/bin/sas get_status</code>. Works immediately, no profile editing required.</p></li></ol><p>You can switch to the admin-backed system-wide install at any time from <strong>Settings → Developer Tools → sas CLI</strong> — click <strong>Uninstall</strong> (removes the user-local copy without a prompt), then <strong>Install</strong>.</p><h3 id="if-sas-is-not-found-after-install" tabindex="-1"><a class="header-anchor" href="#if-sas-is-not-found-after-install" aria-hidden="true">#</a> If <code>sas</code> is not found after install</h3><p>The install writes to two places: <code>/usr/local/bin/sas</code> (the wrapper) and <code>/etc/paths.d/signals-and-sorcery</code> (for shells that don&#39;t have <code>/usr/local/bin</code> on PATH by default, like fish). A few scenarios can still leave <code>sas</code> out of reach:</p><table><thead><tr><th>Symptom</th><th>Fix</th></tr></thead><tbody><tr><td><code>zsh: command not found: sas</code> in a terminal you had open during install</td><td>Open a <em>new</em> terminal — the old shell still has the pre-install PATH</td></tr><tr><td><em>New</em> terminal also says <code>command not found</code></td><td>Open <strong>Settings → Developer Tools → sas CLI</strong> and click <strong>Reinstall</strong>. The status line will read <code>stale</code> or <code>not-installed</code> and the fix is one click.</td></tr><tr><td>Preferences shows <code>Status: stale — the app moved since install</code></td><td>Click <strong>Reinstall</strong> — the wrapper hard-codes the app path at install time; moving the app bundle invalidates the wrapper.</td></tr><tr><td>Preferences shows <code>Status: managed by Homebrew</code> (or Nix, or another app)</td><td>Remove the foreign <code>/usr/local/bin/sas</code> first (<code>brew uninstall …</code>), then click Install. The app refuses to clobber binaries it didn&#39;t write.</td></tr><tr><td>You use a non-default shell with a custom PATH that drops <code>/usr/local/bin</code></td><td>Run <code>export PATH=/usr/local/bin:$PATH</code> to verify the wrapper works, then add that line to your shell rc.</td></tr></tbody></table>`,13),h=s("strong",null,"Can't make the CLI work?",-1),m=s("code",null,"curl",-1),v=s("code",null,"http://localhost:7655/api/v1/execute",-1),b=a(`<p><strong>Windows / Linux</strong>: the auto-installer is macOS-only today (the admin-elevation mechanism differs per platform). Use the HTTP or MCP paths instead — they work uniformly.</p><h2 id="verify" tabindex="-1"><a class="header-anchor" href="#verify" aria-hidden="true">#</a> Verify</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sas health
<span class="token comment"># { &quot;status&quot;: &quot;ok&quot;, &quot;timestamp&quot;: &quot;2026-04-14T...&quot; }</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>If that fails with <em>&quot;Cannot reach S&amp;S API at http://localhost:7655&quot;</em>, the app isn&#39;t running. Start it and try again.</p><h2 id="usage-shape" tabindex="-1"><a class="header-anchor" href="#usage-shape" aria-hidden="true">#</a> Usage shape</h2><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>sas &lt;action&gt; [--key value]...        Run a tool action by name
sas list-actions                     List every registered tool
sas help [action]                    Show top-level help, or per-action help
sas health                           Reachability check
sas events stream                    SSE stream of typed domain events
sas jobs list [--status &lt;s&gt;]         Background jobs
sas jobs get &lt;id&gt;                    One job&#39;s state
sas jobs wait &lt;id&gt; [--timeout &lt;ms&gt;]  Long-poll until a job completes
sas jobs cancel &lt;id&gt;                 Cancel a running job

# Plan-as-artifact surface (recommended for agents)
sas inspect project [--include …]    Read-only project snapshot
sas inspect scene [sceneId]          One scene + its tracks
sas inspect track &lt;trackId&gt;          One track&#39;s mute/solo/vol/pan
sas inspect history [--limit n]      Recent checkpoints
sas plan &lt;intent…&gt; [--plan-out f]    Free-text → typed JSON Plan
sas validate &lt;plan-file|-&gt;           Validate a plan against current state
sas apply &lt;plan-file|-&gt; [--checkpoint name|--dry-run|--skip-checkpoint]
sas preview [sceneId] [--track-id …] [--refresh] [--bpm …] [--bars …]
sas history list [--limit n]         List checkpoints (newest first)
sas history checkpoint &lt;name&gt; [--notes …]   Manual checkpoint
sas history undo &lt;name&gt;              Restore to checkpoint
sas history delete &lt;name&gt;            Drop one checkpoint
sas history prune                    Drop expired checkpoints
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="global-flags" tabindex="-1"><a class="header-anchor" href="#global-flags" aria-hidden="true">#</a> Global flags</h2><table><thead><tr><th>Flag</th><th>Effect</th></tr></thead><tbody><tr><td><code>--pretty</code></td><td>Indent JSON output (default is compact for pipe-friendliness)</td></tr><tr><td><code>--quiet</code></td><td>Suppress error messages on stderr</td></tr><tr><td><code>--api &lt;url&gt;</code></td><td>Override API base URL (also via <code>$SAS_API_URL</code>)</td></tr><tr><td><code>-h</code>, <code>--help</code></td><td>Top-level help, or per-action help if passed after an action name</td></tr></tbody></table><h2 id="argument-conventions" tabindex="-1"><a class="header-anchor" href="#argument-conventions" aria-hidden="true">#</a> Argument conventions</h2><ul><li><strong>Kebab-case flags → camelCase params:</strong> <code>--scene-id abc</code> maps to <code>sceneId: &quot;abc&quot;</code> in the underlying tool call.</li><li><strong>Booleans:</strong> <code>--enabled</code> (true), <code>--no-enabled</code> or <code>--enabled=false</code> (false).</li><li><strong>Numbers:</strong> <code>--bpm 90</code> — coerced from the tool&#39;s input schema; non-numeric values error out early with exit 2.</li><li><strong>Arrays:</strong> repeat the flag — <code>--paths a.wav --paths b.wav</code>.</li><li><strong>Nested objects (escape hatch):</strong> <code>--json &#39;{&quot;key&quot;:&quot;value&quot;}&#39;</code>.</li></ul><h2 id="exit-codes" tabindex="-1"><a class="header-anchor" href="#exit-codes" aria-hidden="true">#</a> Exit codes</h2><table><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>0</code></td><td>Success</td></tr><tr><td><code>1</code></td><td>Tool returned <code>success: false</code>, OR a runtime error (engine offline, bad JSON, etc.)</td></tr><tr><td><code>2</code></td><td>Argument parsing failure (bad flag, missing required value, bad type)</td></tr></tbody></table><p>This means <code>set -e</code> works in shell scripts — a failing tool stops the script unless you explicitly handle it.</p><h2 id="tool-discovery" tabindex="-1"><a class="header-anchor" href="#tool-discovery" aria-hidden="true">#</a> Tool discovery</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Every action</span>
sas list-actions

<span class="token comment"># One action&#39;s full help (parameters + when-to-use)</span>
sas <span class="token builtin class-name">help</span> compose_scene
sas <span class="token builtin class-name">help</span> dsl_set_track_fx
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,15),g={href:"https://github.com/shiehn/sas-platform/blob/main/sas-assistant/docs-ai-planning/ai-orchestration-design.md#236-tool-description-template",target:"_blank",rel:"noopener noreferrer"},k=a(`<ul><li><strong>WHEN TO USE</strong> — scenarios that fit this tool</li><li><strong>WHEN NOT TO USE</strong> — when another tool fits better (named)</li><li><strong>INPUTS</strong> — parameter list with example values</li><li><strong>OUTPUTS</strong> — success / failure envelope shape and emitted events</li></ul><h2 id="progressive-disclosure" tabindex="-1"><a class="header-anchor" href="#progressive-disclosure" aria-hidden="true">#</a> Progressive disclosure</h2><p>By default <code>list-actions</code> returns the curated core tool set (~24 scene-scoped verbs covering create, mix, transport, scene navigation, plus the plan-loop verbs). Less-common tools (samples, export, advanced scene plumbing, etc.) are <em>deferred</em> — agents discover them via <code>tool_search</code>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Agent: I need something to export audio. Let me search.</span>
sas tool_search <span class="token parameter variable">--query</span> <span class="token string">&quot;export wav&quot;</span> <span class="token parameter variable">--limit</span> <span class="token number">3</span>
<span class="token comment"># Returns matches ranked by name + description relevance, with schemas</span>
<span class="token comment"># so the agent can invoke directly.</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The same default-curated set is what the in-app chat-plugin agent sees — <code>/api/v1/actions</code> (used by the <code>sas</code> CLI) and <code>host.listAppTools</code> (used by the chat-plugin) share a single filter implementation. Adding a tool to the registry exposes it on both surfaces atomically; promoting a deferred tool reaches both at once.</p><h3 id="filter-parameters" tabindex="-1"><a class="header-anchor" href="#filter-parameters" aria-hidden="true">#</a> Filter parameters</h3><table><thead><tr><th>Query</th><th>Effect</th></tr></thead><tbody><tr><td><em>(none)</em></td><td>Curated default — non-deferred tools across all scopes</td></tr><tr><td><code>?scope=scene</code></td><td>Non-deferred, scene-scoped only (mirrors the chat-plugin&#39;s default)</td></tr><tr><td><code>?scope=project</code></td><td>Non-deferred, project-scoped only</td></tr><tr><td><code>?include_deferred=true</code></td><td>All registered tools incl. deferred</td></tr><tr><td><code>?all=true</code></td><td>Legacy alias of <code>?include_deferred=true</code></td></tr></tbody></table><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># What the chat-plugin&#39;s agent sees by default</span>
<span class="token function">curl</span> <span class="token string">&#39;http://localhost:7655/api/v1/actions?scope=scene&#39;</span>

<span class="token comment"># Every registered tool (admin/debug visibility)</span>
<span class="token function">curl</span> <span class="token string">&#39;http://localhost:7655/api/v1/actions?include_deferred=true&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="events" tabindex="-1"><a class="header-anchor" href="#events" aria-hidden="true">#</a> Events</h2><p>Every mutating tool emits typed domain events. Stream them to react in real time:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Raw JSON event stream (one event per line)</span>
sas events stream

<span class="token comment"># Filter for specific event types</span>
sas events stream <span class="token operator">|</span> <span class="token function">grep</span> <span class="token string">&#39;track:created&#39;</span>

<span class="token comment"># Pretty-print with jq</span>
sas events stream <span class="token operator">|</span> jq <span class="token parameter variable">-r</span> <span class="token string">&#39;select(.event == &quot;domainEvent&quot;) | .data&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Event types include: <code>scene:created</code>, <code>scene:activated</code>, <code>track:created</code>, <code>track:midi-written</code>, <code>track:fx-changed</code>, <code>bpm:changed</code>, <code>deck:state-changed</code>, <code>sample:imported</code>, <code>transition:created</code>, and more.</p><h2 id="jobs" tabindex="-1"><a class="header-anchor" href="#jobs" aria-hidden="true">#</a> Jobs</h2><p>Long-running tools (like <code>compose_scene</code>) open a <code>JobManager</code> job so consumers can watch progress:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Start a compose and capture the response</span>
<span class="token assign-left variable"><span class="token environment constant">JOB</span></span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span>sas compose_scene <span class="token parameter variable">--description</span> <span class="token string">&quot;chill lo-fi&quot;</span> --scene-name <span class="token string">&quot;Verse&quot;</span> <span class="token punctuation">\\</span>
  <span class="token parameter variable">--json</span> <span class="token string">&#39;{&quot;tracks&quot;:[{&quot;name&quot;:&quot;Bass&quot;,&quot;role&quot;:&quot;bass&quot;,&quot;prompt&quot;:&quot;deep slow&quot;}]}&#39;</span> <span class="token punctuation">\\</span>
  <span class="token operator">|</span> jq <span class="token parameter variable">-r</span> <span class="token string">&#39;.jobId // empty&#39;</span><span class="token variable">)</span></span>

<span class="token comment"># If job-based, wait for completion</span>
<span class="token punctuation">[</span> <span class="token parameter variable">-n</span> <span class="token string">&quot;<span class="token environment constant">$JOB</span>&quot;</span> <span class="token punctuation">]</span> <span class="token operator">&amp;&amp;</span> sas <span class="token function">jobs</span> <span class="token function">wait</span> <span class="token string">&quot;<span class="token environment constant">$JOB</span>&quot;</span> <span class="token parameter variable">--timeout</span> <span class="token number">60000</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Even without <code>jobs wait</code>, the tool returns the final result when the composite finishes — the job is for progress observers, not callers.</p><h2 id="idempotency-keys" tabindex="-1"><a class="header-anchor" href="#idempotency-keys" aria-hidden="true">#</a> Idempotency keys</h2><p>All mutating tools accept a top-level <code>--idempotency-key</code>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Same key + same params = same result (cached within 60s, per project)</span>
sas dsl_track_create --idempotency-key <span class="token string">&quot;retry-abc-1&quot;</span> <span class="token parameter variable">--name</span> <span class="token string">&quot;Bass&quot;</span> <span class="token parameter variable">--role</span> bass
sas dsl_track_create --idempotency-key <span class="token string">&quot;retry-abc-1&quot;</span> <span class="token parameter variable">--name</span> <span class="token string">&quot;Bass&quot;</span> <span class="token parameter variable">--role</span> bass
<span class="token comment"># ↑ second call returns the first&#39;s result — no duplicate track</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,19),q={href:"https://github.com/shiehn/sas-platform/blob/main/sas-assistant/docs-ai-planning/ai-orchestration-design.md",target:"_blank",rel:"noopener noreferrer"},f=a(`<h2 id="the-json-escape-hatch" tabindex="-1"><a class="header-anchor" href="#the-json-escape-hatch" aria-hidden="true">#</a> The <code>--json</code> escape hatch</h2><p>For tools with complex nested inputs (like <code>compose_scene</code> which takes a <code>tracks</code> array), pass them as JSON directly:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sas compose_scene <span class="token punctuation">\\</span>
  <span class="token parameter variable">--description</span> <span class="token string">&quot;chill lo-fi&quot;</span> <span class="token punctuation">\\</span>
  --scene-name <span class="token string">&quot;Verse&quot;</span> <span class="token punctuation">\\</span>
  <span class="token parameter variable">--json</span> <span class="token string">&#39;{
    &quot;tracks&quot;: [
      {&quot;name&quot;: &quot;Bass&quot;,  &quot;role&quot;: &quot;bass&quot;,  &quot;prompt&quot;: &quot;deep, slow lo-fi&quot;},
      {&quot;name&quot;: &quot;Drums&quot;, &quot;role&quot;: &quot;drums&quot;, &quot;prompt&quot;: &quot;laid-back swung&quot;},
      {&quot;name&quot;: &quot;Keys&quot;,  &quot;role&quot;: &quot;chords&quot;,&quot;prompt&quot;: &quot;jazzy extensions&quot;}
    ]
  }&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="plan-as-artifact-surface" tabindex="-1"><a class="header-anchor" href="#plan-as-artifact-surface" aria-hidden="true">#</a> Plan-as-artifact surface</h2><p>Granular tools (<code>scene_create</code>, <code>dsl_track_create</code>, …) remain available and stable, but the <strong>recommended path for agents</strong> is the six-verb plan-as-artifact loop:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>inspect → plan → validate → apply → preview → undo
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Each verb is its own subcommand; together they let an agent reason about the project, propose a typed change, check it against current state, mutate the world reversibly, hear the result, and roll back without losing data.</p><h3 id="sas-inspect-—-read-only-views" tabindex="-1"><a class="header-anchor" href="#sas-inspect-—-read-only-views" aria-hidden="true">#</a> <code>sas inspect …</code> — read-only views</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sas inspect project                     <span class="token comment"># everything: scenes, tracks, context, history</span>
sas inspect project <span class="token parameter variable">--include</span> scenes,tracks
sas inspect scene                       <span class="token comment"># active scene</span>
sas inspect scene <span class="token operator">&lt;</span>sceneId<span class="token operator">&gt;</span>             <span class="token comment"># specific scene</span>
sas inspect track <span class="token operator">&lt;</span>trackId<span class="token operator">&gt;</span>             <span class="token comment"># one track&#39;s surface state</span>
sas inspect <span class="token function">history</span> <span class="token parameter variable">--limit</span> <span class="token number">10</span>          <span class="token comment"># recent checkpoints</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>inspect</code> never mutates. The output is structured JSON in <code>--json</code> mode; human mode prints compact summaries. Names are resolved from UUIDs so agents can chain conversationally without a second lookup.</p><h3 id="sas-plan-intent-—-emit-a-typed-json-plan" tabindex="-1"><a class="header-anchor" href="#sas-plan-intent-—-emit-a-typed-json-plan" aria-hidden="true">#</a> <code>sas plan &lt;intent…&gt;</code> — emit a typed JSON Plan</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Free-text intent → typed plan, printed to stdout</span>
sas plan <span class="token string">&quot;make me a chill lo-fi beat&quot;</span>

<span class="token comment"># Save the plan for later</span>
sas plan <span class="token string">&quot;make me a chill lo-fi beat&quot;</span> --plan-out beat.plan.json

<span class="token comment"># Force a specific PlanType (when goal-router would guess wrong)</span>
sas plan <span class="token string">&quot;add a sub bass&quot;</span> <span class="token parameter variable">--type</span> track_revise

<span class="token comment"># Legacy Phase 4 prereq-chain preview (no typed plan, just the chain)</span>
sas plan <span class="token string">&quot;play the scene&quot;</span> --chain-only
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The plan is the <strong>contract</strong>: a JSON document the agent can read, edit, explain to the user, and hand to <code>validate</code> / <code>apply</code>. Plan shape lives at <code>src/shared/types/agent-plan.ts</code> and is versioned via <code>metadata.plan_schema_version</code> (currently <code>1</code>).</p><p>Top-level shape:</p><div class="language-jsonc line-numbers-mode" data-ext="jsonc"><pre class="language-jsonc"><code>{
  &quot;id&quot;: &quot;plan-scene_create-1714850000-abc123&quot;,
  &quot;intent&quot;: &quot;make me a chill lo-fi beat&quot;,
  &quot;type&quot;: &quot;scene_create&quot;,
  &quot;preconditions&quot;: { &quot;project_bound&quot;: true },
  &quot;steps&quot;: [
    { &quot;id&quot;: &quot;plan-…0.scene_create&quot;,     &quot;type&quot;: &quot;scene_create&quot;,     &quot;inputs&quot;: { &quot;name&quot;: &quot;lo-fi&quot; } },
    { &quot;id&quot;: &quot;plan-…1.dsl_track_create&quot;, &quot;type&quot;: &quot;dsl_track_create&quot;, &quot;inputs&quot;: { &quot;name&quot;: &quot;Bass&quot;, &quot;role&quot;: &quot;bass&quot; } }
  ],
  &quot;rollback&quot;: { &quot;strategy&quot;: &quot;checkpoint_undo&quot; },
  &quot;metadata&quot;: {
    &quot;created_at&quot;: &quot;2026-05-04T15:00:00.000Z&quot;,
    &quot;created_by&quot;: &quot;cli&quot;,
    &quot;plan_schema_version&quot;: 1
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>PlanTypes recognized today: <code>scene_create</code>, <code>scene_revise</code>, <code>track_revise</code>, <code>transition_create</code>, <code>mix_balance</code>, <code>render_preview</code>, <code>composite</code>.</p><h3 id="sas-validate-plan-file-—-check-before-apply" tabindex="-1"><a class="header-anchor" href="#sas-validate-plan-file-—-check-before-apply" aria-hidden="true">#</a> <code>sas validate &lt;plan-file|-&gt;</code> — check before apply</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sas validate beat.plan.json
sas plan <span class="token string">&quot;make a beat&quot;</span> --plan-out /tmp/p.json <span class="token operator">&amp;&amp;</span> sas validate /tmp/p.json

<span class="token comment"># Pipe directly — validate reads stdin when the file arg is &quot;-&quot;</span>
sas plan <span class="token string">&quot;make a beat&quot;</span> <span class="token parameter variable">--json</span> <span class="token operator">|</span> jq <span class="token string">&#39;.data.changes.plan&#39;</span> <span class="token operator">|</span> sas validate -
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Returns a <code>PlanValidationResult</code>:</p><div class="language-jsonc line-numbers-mode" data-ext="jsonc"><pre class="language-jsonc"><code>{
  &quot;valid&quot;: false,
  &quot;errors&quot;: [
    {
      &quot;path&quot;: &quot;$.preconditions.project_bound&quot;,
      &quot;code&quot;: &quot;missing_precondition&quot;,
      &quot;message&quot;: &quot;No project is bound — open or create one first.&quot;,
      &quot;suggestedFix&quot;: { &quot;tool&quot;: &quot;list_projects&quot;, &quot;args&quot;: {} }
    }
  ],
  &quot;warnings&quot;: [],
  &quot;preview&quot;: {
    &quot;wouldCreate&quot;: { &quot;scenes&quot;: 1, &quot;tracks&quot;: 4 },
    &quot;riskLevel&quot;: &quot;medium&quot;,
    &quot;requiresConfirmation&quot;: false
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Exit codes:</p><ul><li><code>0</code> — valid, no errors</li><li><code>1</code> — invalid (one or more errors); script can branch on this</li><li><code>2</code> — bad input (file not found, malformed JSON)</li></ul><p><code>suggestedFix</code> is the agent&#39;s recovery hook: it points at the exact tool + args that would unblock the failed precondition, so the agent can self-correct without re-prompting the user.</p><h3 id="sas-apply-plan-file-—-execute-reversibly" tabindex="-1"><a class="header-anchor" href="#sas-apply-plan-file-—-execute-reversibly" aria-hidden="true">#</a> <code>sas apply &lt;plan-file|-&gt;</code> — execute reversibly</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Auto-checkpoint pre-apply (default). Restorable via \`sas history undo\`.</span>
sas apply beat.plan.json

<span class="token comment"># Override the checkpoint name</span>
sas apply beat.plan.json <span class="token parameter variable">--checkpoint</span> pre-techno

<span class="token comment"># Validate-only mode; print the preview block, don&#39;t mutate</span>
sas apply beat.plan.json --dry-run

<span class="token comment"># Skip the checkpoint entirely (caller handles undo themselves)</span>
sas apply beat.plan.json --skip-checkpoint

<span class="token comment"># Pipe from \`plan\` directly</span>
sas plan <span class="token string">&quot;make me a beat&quot;</span> <span class="token parameter variable">--json</span> <span class="token operator">|</span> jq <span class="token string">&#39;.data.changes.plan&#39;</span> <span class="token operator">|</span> sas apply -
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Default behavior:</p><ol><li>Validate the plan. If invalid, exit <code>1</code> with the error list.</li><li><strong>Auto-create a checkpoint</strong> named <code>pre-apply-&lt;plan.id&gt;-&lt;ts&gt;</code> capturing DB rows + engine surface state (mute/solo/volume/pan/plugin state).</li><li>Execute steps sequentially. Each step&#39;s <code>outputs</code> resolve <code>\${steps.&lt;id&gt;.outputs.&lt;key&gt;}</code> references in later step <code>inputs</code>.</li><li>On any step failure, fire <code>compensate</code> hooks LIFO and return <code>failed_step_id</code> + <code>rolled_back_to</code>. The checkpoint is preserved so the user can recover with <code>sas history undo</code>.</li></ol><p>Idempotent: re-running an interrupted plan replays from the last non-completed step. Step ids are deterministic (<code>\${plan.id}.\${idx}.\${type}</code>).</p><h3 id="sas-preview-sceneid-—-render-audio" tabindex="-1"><a class="header-anchor" href="#sas-preview-sceneid-—-render-audio" aria-hidden="true">#</a> <code>sas preview [sceneId]</code> — render audio</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sas preview                              <span class="token comment"># active scene</span>
sas preview <span class="token operator">&lt;</span>sceneId<span class="token operator">&gt;</span>
sas preview --track-id <span class="token operator">&lt;</span>trackId<span class="token operator">&gt;</span>         <span class="token comment"># bounce just this track</span>
sas preview <span class="token operator">&lt;</span>sceneId<span class="token operator">&gt;</span> <span class="token parameter variable">--refresh</span>          <span class="token comment"># force re-render (skip cache)</span>
sas preview <span class="token operator">&lt;</span>sceneId<span class="token operator">&gt;</span> <span class="token parameter variable">--bpm</span> <span class="token number">120</span> <span class="token parameter variable">--bars</span> <span class="token number">8</span> <span class="token comment"># render-time overrides</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Returns:</p><div class="language-jsonc line-numbers-mode" data-ext="jsonc"><pre class="language-jsonc"><code>{
  &quot;audio&quot;: {
    &quot;url&quot;: &quot;file:///…/render-cache/&lt;hash&gt;.wav&quot;,
    &quot;durationSeconds&quot;: 7.74,
    &quot;sampleRate&quot;: 48000,
    &quot;contentHash&quot;: &quot;sha256:…&quot;,
    &quot;summary&quot;: &quot;4 tracks · 4 bars @ 90 BPM&quot;,
    &quot;cacheHit&quot;: true,
    &quot;staleness&quot;: &quot;fresh&quot;
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Backed by the content-addressable render cache. <code>staleness</code> values:</p><table><thead><tr><th>Value</th><th>Meaning</th></tr></thead><tbody><tr><td><code>fresh</code></td><td>Render cache hit; the WAV reflects current state</td></tr><tr><td><code>stale_render</code></td><td>Cache exists but content hash drifted; pass <code>--refresh</code> to rebuild</td></tr><tr><td><code>no_render</code></td><td>First render request — <code>--refresh</code> not needed; we&#39;ll build it</td></tr><tr><td><code>rendered_now</code></td><td>We just rendered for this call</td></tr></tbody></table><p>Per-track preview uses the C++ <code>trackIds</code> filter in <code>SceneRenderer.cpp</code> to bounce one track in isolation — handy for A/B-ing a <code>track_revise</code> plan before applying it.</p><h3 id="sas-history-—-checkpoints-undo" tabindex="-1"><a class="header-anchor" href="#sas-history-—-checkpoints-undo" aria-hidden="true">#</a> <code>sas history …</code> — checkpoints + undo</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sas <span class="token function">history</span> list <span class="token parameter variable">--limit</span> <span class="token number">10</span>                       <span class="token comment"># newest first</span>
sas <span class="token function">history</span> checkpoint pre-experiment             <span class="token comment"># manual save point</span>
sas <span class="token function">history</span> checkpoint pre-experiment <span class="token parameter variable">--notes</span> <span class="token string">&quot;before mix tweaks&quot;</span>
sas <span class="token function">history</span> undo pre-experiment                   <span class="token comment"># restore</span>
sas <span class="token function">history</span> delete pre-experiment                 <span class="token comment"># drop one</span>
sas <span class="token function">history</span> prune                                 <span class="token comment"># drop expired (default TTL 24h)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>undo</code> runs in a single SQLite transaction + sequence of engine RPCs. Render cache entries are content-addressable and survive undo independently — restoring scene state hits the cache immediately when you <code>sas preview</code> after the undo.</p><p>Audio bounces are <strong>not</strong> included in checkpoints. To preserve a render explicitly, run <code>sas preview</code> (or <code>render_to_performance</code>) before the checkpoint — it lands in the cache and stays there.</p><h3 id="universal-flags-for-plan-apply-preview" tabindex="-1"><a class="header-anchor" href="#universal-flags-for-plan-apply-preview" aria-hidden="true">#</a> Universal flags for plan/apply/preview</h3>`,40),y={href:"https://clig.dev/",target:"_blank",rel:"noopener noreferrer"},w=a("<table><thead><tr><th>Flag</th><th>All commands</th><th>Mutating</th><th>Apply-only</th></tr></thead><tbody><tr><td><code>--json</code></td><td>yes</td><td>yes</td><td>yes</td></tr><tr><td><code>--no-color</code></td><td>yes</td><td>yes</td><td>yes</td></tr><tr><td><code>--verbose</code></td><td>yes</td><td>yes</td><td>yes</td></tr><tr><td><code>--dry-run</code></td><td>—</td><td>yes</td><td>yes</td></tr><tr><td><code>--plan-out &lt;file&gt;</code></td><td>—</td><td>yes (<code>plan</code>)</td><td>—</td></tr><tr><td><code>--checkpoint &lt;name&gt;</code></td><td>—</td><td>—</td><td>yes</td></tr><tr><td><code>--skip-checkpoint</code></td><td>—</td><td>—</td><td>yes</td></tr></tbody></table>",1);function x(_,j){const i=o("RouterLink"),t=o("ExternalLinkIcon");return d(),l("div",null,[u,s("p",null,[h,e(" You don't need it. The CLI is a thin wrapper around the local HTTP API — "),m,e(" against "),v,e(" gives you every action the CLI has. See the "),n(i,{to:"/automation/#quick-start"},{default:c(()=>[e("automation overview")]),_:1}),e(" for worked examples.")]),b,s("p",null,[e("Help output follows the "),s("a",g,[e("4-section template"),n(t)]),e(" every tool is enforced to have:")]),k,s("p",null,[e("Safe to retry on transient errors without corrupting state. See the "),s("a",q,[e("AI orchestration design doc"),n(t)]),e(" § 8 for the full spec.")]),f,s("p",null,[e("Following "),s("a",y,[e("clig.dev"),n(t)]),e(", every plan-shaped command accepts:")]),w])}const I=r(p,[["render",x],["__file","cli-reference.html.vue"]]);export{I as default};
